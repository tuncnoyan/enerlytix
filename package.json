{
  "name": "enerlytix",
  "version": "1.0.0",
  "private": true,
  "description": "Deployment manifest for Lovable compatibility.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node -e \"console.log('No Node build step required for this Django project.')\"",
    "start": "node -e \"console.log('Application runtime is Django. Start with: cd django_app && python manage.py runserver 0.0.0.0:8080')\"",
    "dev": "npm run start"
  }
}
